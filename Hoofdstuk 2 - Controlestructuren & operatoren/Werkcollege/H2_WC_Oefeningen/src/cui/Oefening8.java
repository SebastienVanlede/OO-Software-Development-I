package cui;

import java.util.Scanner;

public class Oefening8 {

	public static void main(String[] args) {
		Scanner input = new Scanner(System.in);

		// A
		/*
		 * int getal, x, som = 0;
		 * 
		 * for (x = 1; x <= 5; x++) { System.out.printf("Geef getal %d: ", x); getal =
		 * input.nextInt(); som += getal; }
		 * 
		 * System.out.printf("De som van de getallen is %d%n", som);
		 */

		// B
		/*
		 * int getal, x , deelbaar = 0, even = 0;
		 * 
		 * for (x = 1; x <= 5; x++) { System.out.printf("Geef getal %d: ", x); getal =
		 * input.nextInt(); if (getal % 2 == 0) even++; if (getal % 3 == 0) deelbaar++;
		 * }
		 * 
		 * System.out.
		 * printf("Je gaf %d even getallen en %d getallen deelbaar door 3 in%n", even,
		 * deelbaar);
		 */
		
		
	}

}
